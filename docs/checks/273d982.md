## `273d982` — every director sheet remembers itself on reload

> reload after opening a sheet on one of its inner tabs — does the director return there, fully drawn, not the wave editor

- **badge** implementation
- **subject** the director's own overlay sheets — NOT BUILT YET, TO CHECK, STATES, SOUND, CONTROL SETS — and each one's inner tab bar
- **changed** only the main WAVE/SHIP/TUNING/BALANCE tab and the wave index survived a reload before; now the sheet that was open and its own inner tab do too, restored through the same open button and tab button a person would click rather than a class toggle that would skip a sheet's own draw
- **decide** open one of the sheets, pick a tab inside it that is not the default, reload — does the director come back on that sheet, on that tab, with the tab's own content already drawn rather than blank?
- **before** any reload lands on the wave editor, whatever sheet or tab was open
- **after** the sheet and its inner tab are named in the URL and clicked back open on load
- **where** `bun run dev`, open NOT BUILT YET or SOUND, pick a tab that is not the first, reload
