## `34440bc` — a meteor's craters catch the key light

> is the meteor grey now, at speed, against the field's own violet

- **badge** implementation
- **subject** the rock's own colour and the pixels around it in the running field
- **changed** sampled a live meteor at speed across many spins and sizes; every pixel measured stayed cool blue-grey, so the fill and light were left as they were
- **decide** at tempo, against the field's own violet backdrop, does the rock still read as grey rather than as tinted?
- **before** `docs/checks/34440bc-before.png` — the shipped rock, unlit craters
- **after** `docs/checks/34440bc-after.png` — same rock, still the same grey fill and light
- **where** `bun run preview`, THE ROCK

> does the meteor read as a rock rather than as a circle with dots on it — at 26 px on a phone, beside a bulb for contrast

- **badge** implementation
- **subject** the pits a shot leaves in a meteor's own surface
- **changed** each pit was a flat dark disc with a uniform ring; each now has a floor and a rim shaded along the rock's own light, so one pit can sit brighter than another
- **decide** at 26 px, does a pitted rock read as stone rather than as a dice face?
- **before** `docs/checks/34440bc-before.png` — flat dark discs, one uniform grey ring each
- **after** `docs/checks/34440bc-after.png` — floor and rim graded along the key light, blended into the rock's own shading
- **where** `bun run preview`, THE ROCK
