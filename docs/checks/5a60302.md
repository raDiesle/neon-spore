## `5a60302` — the not-built-yet page's own answer, pulled out of the note fog

> opening a director page you have never used, does the first thing you read tell you what it is for?

- **badge** implementation
- **subject** every NOT BUILT YET tab, plus the renamed ALTERNATIVES sheet
- **changed** each tab's own explanation is now a bigger, brighter, sticky line under the tab bar, not a 9px dim caption buried above a wall of cards
- **decide** does the sub-line read before the cards do, on a tab you have never opened, without hunting for it?
- **before** any NOT BUILT YET tab before this change, or `main`'s VERSUS tab
- **after** the same tab now, and the ALTERNATIVES tab in its place
- **where** `DIRECTOR_HOST=127.0.0.1 bun run dev`, ◇ NOT BUILT YET, any tab
