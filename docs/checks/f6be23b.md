## `f6be23b` — A wave opens on its name

> does a wave now open on its number, name and sentence as plain text on the field — long enough to read, without a panel around it — and does the guide that follows still read as two halves that have to be spoken across

- **badge** implementation
- **subject** How a wave opens, before anything falls
- **changed** A wave now shows its number, name and sentence as bare text on the field, then the help it carries, then the wave. The fading banner is gone.
- **decide** Whether the three lines stand long enough to read, and whether the help behind them still reads as two halves of one instruction.
- **before** a name and a hint fading over a wave already running — `docs/checks/frames/f6be23b/before.png`
- **after** three lines on a still field, five and a half seconds, then a split screen that waits for both — `docs/checks/frames/f6be23b/after.png`
- **where** `DIRECTOR_HOST=127.0.0.1 bun run dev`, the stage in test mode on wave 4, or the game itself on two phones
