## `0e1a62f` — two slots for the shot and the ward

> does either alternative read as something leaving the ship, rather than as a brighter flash where it started?

- **badge** concept
- **subject** two candidate looks for how the cannon's ordinary shot travels
- **changed** the shot's alphas, widths and radii were literals; they are now `SHOT_LOOK`, and two candidates patch it
- **decide** at 380 px and at tempo, does either one read as a thing leaving the ship rather than a flash?
- **before** a sawtooth tail back to the tile just left, a halo and a small solid head
- **after** `streak`, a constant 2.6-tile line with almost no head; `pip`, no tail and a white edge
- **where** `bun run dev` → VERSUS → `cannon:shot`

> can you tell the shield caught something without watching the thing that hit it?

- **badge** concept
- **subject** two candidate looks for the armed rim and the moment of a catch
- **changed** the rim, the press and the shockwave were module constants; they are now `WARD_LOOK` and `DEFLECT_LOOK`, and two candidates patch both
- **decide** with your eye anywhere but the falling rock, does either one announce the catch?
- **before** a bright shimmering band and one shockwave arc that grows and fades
- **after** `heave`, a wide slow rim that gives deeply; `tick`, a hairline rim and three hard rings
- **where** `bun run dev` → VERSUS → `shield:ward`
