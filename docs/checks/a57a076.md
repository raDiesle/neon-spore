## `a57a076` — the director's own panel

> pick LANCE PANEL on one wave and STANDARD on the next — does the panel under the stage actually change when you switch between them

- **badge** implementation
- **subject** the control band drawn under the director's stage while editing a wave
- **changed** the band and THE GAUGE's slabs now draw the panel the wave editor's own picker names, instead of always the shipped wave's panel at that position
- **decide** does the drawn band actually change when the picker is switched between STANDARD and LANCE PANEL, on the same wave?
- **before** every wave showed whatever the shipped campaign's wave at that number happened to name — usually the lance
- **after** the band should follow the picker's own choice, wave by wave
- **where** `DIRECTOR_HOST=127.0.0.1 bun run dev`, the WAVE tab's CONTROL SET field
