## `916f811` — the gauge's controls answer a mouse

> in the director, does clicking and holding LEFT or RIGHT under the gauge actually turn the needle, and does CALL score a mark

- **badge** implementation
- **subject** the three slabs under THE GAUGE in the director — LEFT, RIGHT and CALL
- **changed** they were drawn and nothing listened; a press now sends the round's own command, and Z, X and C do it from the keyboard
- **decide** does holding LEFT or RIGHT move the needle, and does CALL between the marks score one of the five?
- **before** every click on a slab did nothing at all, on any pointer
- **after** the needle turns while held and stops when let go; CALL scores or misses
- **where** `DIRECTOR_HOST=127.0.0.1 bun run dev`, wave 11 IN ITS SHADOW, the INTERLUDE tab, `▶ PLAY`
