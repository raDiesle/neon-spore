## `10dda25` — the stage steps a card

> in the director, does clicking the field step a card through player 1's half, then player 2's, then start the wave — and does the first click after that move the cannon rather than the card

- **badge** implementation
- **subject** the briefing card in the director's test mode
- **changed** `✓ CARD` button removed; a click on the field itself now steps a test-mode card through player one's half, then player two's, then dismisses it and plays the wave
- **decide** does clicking the field read as the same control the phone has, and does the sequence land on the cannon rather than fumbling for a button?
- **before** a card in test mode showed both halves at once, dismissed only by a separate `✓ CARD` button
- **after** clicking the field steps p1 half → p2 half → dismiss/play, matching what a phone screen would show each seat
- **where** `DIRECTOR_HOST=127.0.0.1 bun run dev`, TEST role, any wave with a card due, clicked at tempo
