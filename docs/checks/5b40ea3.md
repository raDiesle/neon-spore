## `5b40ea3` — the topbar's second copy of the keys

> open the director — is the key list gone from the top bar, and does the KEYS button still tell you everything it used to say, including that you can play the stage with the mouse?

- **badge** implementation
- **subject** the key list that used to sit in the top-right of the topbar
- **changed** the topbar's hand-typed key list is gone; the KEYS popup now also says you can play the stage with the mouse, which the topbar said and the popup did not
- **decide** does the top bar read clean without the key list, and does the KEYS popup still tell you everything the old line did?
- **before** the topbar carrying "A/D cannon · J/L shield · I guard · S maw · W/E fire · G grip (as P2) · or play the stage with the mouse" beside six buttons
- **after** the topbar ending at GUIDES; the KEYS popup listing every key by seat, plus a line about the mouse
- **where** `DIRECTOR_HOST=127.0.0.1 bun run dev`, topbar, then the ⌨ KEYS button (bottom of the WAVE tab's stage controls)
