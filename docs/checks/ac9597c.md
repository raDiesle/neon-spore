## `ac9597c` — the egg-laying mouth, adopted

> does the laying read at tempo in the game — strain, crown and the slack afterwards, not only a shot arriving

- **badge** implementation
- **subject** the cannon's fire opening, `packages/render/src/muzzle.ts`'s `MOUTH_LOOK` and `packages/render/src/cannon-maw.ts`'s `LAY_LOOK`, adopted from the `egg` candidate the owner asked for by name
- **changed** the round port that only appeared mid-shot and cut the instant it left is gone. The mouth is a cloaca now, drawn on every frame the maw is not swallowing a pod: it strains as a press comes due, crowns as the shot comes through, and goes slack afterwards rather than vanishing. `shotChargeBeats` is unchanged — 0.5 in `apps/game`, 0 in `DEFAULT_CONFIG`
- **decide** watch a shot leave at tempo: does the strain-crown-slack sequence read as effort, or does the half a beat it has to happen in go by too fast to follow?
- **before** the old opening — a dark ellipse that appeared and a rim that tightened only while a shot was in flight
- **after** the cloaca — strains, crowns, goes slack; present at rest as well
- **where** `bun run preview`, in a worktree by absolute path, confirmed via `curl -s http://localhost:<port>/__preview` — then fire a shot and watch the mouth, not the bolt

Not watched at tempo in this session: keyboard-driven fire input did not reach
the simulation in the automated browser (`apps/game/src/keys.ts`'s `KeyQ`
never changed `window.neonSpore.world`, checked both as a real key press and
as a dispatched `KeyboardEvent`, on a fresh load and inside a "PLAY ALONE"
solo session). The attached frame was produced by setting
`window.neonSpore.world.charge` directly — the same field the sim's own
`fire()` sets — and calling the real `window.neonSpore.paint()`, so it is the
shipped renderer drawing a real World state, just one reached by hand rather
than by a press. Worth a look on a machine with a real keyboard against this
build before this check is marked settled.
