## `1e36468` — a reload keeps the tab and wave, and forgets everything else

> after a reload, are you back on the same tab and wave with every setting back at its default?

- **badge** implementation
- **subject** the director's URL now carries the main tab and wave, restoring the place on reload
- **changed** tab and wave index now live in `?tab=&wave=`, written with history.replaceState on every navigation and read once at startup
- **decide** does a reload return you to the same tab and wave while every dial, skin and motion pick is back at default?
- **before** a reload lost the tab, the wave, and asked whether you meant to discard changes
- **after** a reload restores the tab and wave; every setting is back at its default
- **where** `DIRECTOR_HOST=127.0.0.1 bun run dev`, pick a tab and a wave, reload
