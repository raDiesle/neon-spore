## `943c4f4` — a landed check gets a before and an after picture

> does the before/after pair `bun run frames` captures actually read as an obvious difference, not just to the session that wrote the tool?

- **badge** implementation
- **subject** `bun run frames <sha>` — a real screenshot pair for a landed check
- **changed** nothing about the game; a new tool builds the parent and the commit in scratch worktrees and screenshots both
- **decide** open the pair captured from 16efb33 — do the light-shaft bands read as an obvious addition at a glance?
- **before** the field with no light shafts (16efb33's parent, wave 2, tick 300)
- **after** the same field with three diagonal bands crossing behind the hull
- **where** `bun run frames 16efb33 --wave 1 --ticks 300 --out <dir>`, then open `<dir>/before.png` and `<dir>/after.png`
