## `fca8dac` — one body, every skin, every motion

> with twenty skins on one body at once, is it obvious which ones are worth keeping?

- **badge** implementation
- **subject** the SHAPES tab grid: one body in every skin at once
- **changed** SHAPES showed one skin at a time across sixty bodies; it now also shows one chosen body wearing all nineteen skins side by side
- **decide** seeing all nineteen skins on one body at once, is it obvious which are worth keeping and which are weak copies of another?
- **before** nothing — only one skin at a time, on many bodies
- **after** the EVERY SKIN, ONE BODY row, under the body picker
- **where** `bun run dev` → SHAPES, scroll to EVERY SKIN, ONE BODY

> with every motion on one body at once, can you tell two of them apart without reading the labels?

- **badge** implementation
- **subject** the SHAPES tab grid: one body in every motion at once
- **changed** there was no page showing all eighteen motions side by side; the chosen body now performs all eighteen at once, each labelled
- **decide** covering the labels, can you tell which figure performs which motion, or do several of the eighteen collapse into the same wobble?
- **before** nothing — only one motion at a time, forced on the whole page
- **after** the EVERY MOTION, ONE BODY row, below the skins grid
- **where** `bun run dev` → SHAPES, scroll to EVERY MOTION, ONE BODY
