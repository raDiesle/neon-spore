## `2230f71` — frames looks up a check by what it says, not by its old filename

> does bun run frames work on a commit that landed yesterday?

- **badge** implementation
- **subject** frames' wave lookup joins a commit to its restatement on the trailer's own text
- **changed** deriveWaveFromChecks now reads the commit's Check: trailers and calls findRestatedForCommit, instead of guessing docs/checks/<sha>.md from the landed sha
- **decide** does `bun run frames <sha>` on a commit that landed a day or more ago pick a wave, instead of refusing with "no docs/checks/<sha>.md found"?
- **before** `bun run frames <sha>` refused any commit whose pre-rebase sha did not match its own docs/checks/ filename — 31 of 33
- **after** `bun run frames <sha>` reads the commit's own Check: trailers and finds the restatement that quotes them, filename aside
- **where** `bun run frames 35d59d4` — a commit already on `main`, restated under docs/checks/18036b0.md's pre-rebase name
