## `18036b0` — a piece comes off the shell, and the silhouette says so

> does a piece coming off read as broken away, or as a hole appearing — one at 26 px on a phone, and again beside a meteor for contrast

- **badge** implementation
- **subject** a jagged bite over the exact column whose armour piece just came off THE SHELL
- **changed** shell-draw.ts adds a pass in drawBodies; effects-spark.ts's shellBreak/shellBare now return real bursts instead of null
- **decide** at 26 px on a phone, is it obviously a piece torn away rather than a clean hole or a colour change?
- **before** the shell body drew as one unbroken blob regardless of `Creature.shell`; shellBreak/shellBare threw no burst
- **after** a jagged notch bitten into the armour's own column, redrawn fresh every frame, plus an ember/core-colour burst
- **where** `bun run dev` → THE THIRD SHOT wave; shell-draw.ts's own doc comment
