## `b07f480` — water, or only slow

> does TIDE read as water, or only as slow — the director, TIDE then deepCurrent, with the game's own wave sound over it

- **badge** concept
- **subject** TIDE, a new unreleased music track beside the existing six
- **changed** existing tracks all sit on a countable beat; TIDE layers two slow, unaligned swells with no shared pulse, so nothing to count along with
- **decide** does TIDE read as water — no countable beat — or just as a slower version of the existing on-beat pieces?
- **before** `deepCurrent` — a four-note bass on a strict grid
- **after** TIDE — no bar to count from
- **where** director MUSIC tab, TIDE then `deepCurrent`; then a wave's own sound over it
