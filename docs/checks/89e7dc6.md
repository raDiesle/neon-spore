## `89e7dc6` — LEDGER removal and the bigger field

> on your own screen, is the field now big enough to judge whether the cannon reaches a column in time, with the buttons sitting along the bottom

- **badge** implementation
- **subject** the director's stage column: the LEDGER button and panel, and the field's size
- **changed** removed the duplicate live balance panel and its toggle; the transport row now sits at the bottom of the column and the field takes the rest
- **decide** does the field now read big enough to judge the cannon's timing, with the buttons pinned along the bottom?
- **before** field measured 282×504 px at a 1600×1000 window, with LEDGER and a scrolling note-and-sheet block sharing the column
- **after** field measures 476×851 px at the same window, transport pinned to the bottom, no LEDGER
- **where** `bun run dev`, stage column, transport row
