## `e36f67a` — frames reads where, fixes the wave off-by-one, and refuses an identical pair

> given only a sha, does `bun run frames` open the wave the where field names and refuse rather than screenshot the wrong one?

- **badge** implementation
- **subject** frames now reads where, fixes the wave off-by-one, and refuses an identical pair
- **changed** frames used to default to wave 1 and take an off-by-one --wave; it now reads where, accepts a wave name, and skips identical pairs
- **decide** given only a sha, does `bun run frames` open the wave the where field names and refuse rather than screenshot the wrong one?
- **before** wave 1, always, no matter which sha; `--wave 20` opened the HUD's W21
- **after** the where field's own wave, by number or name; `--wave 21` opens W21; identical pairs are reported, not written
- **where** `bun run frames 18036b0`, `bun run frames 4e577db`, `bun run frames <sha> --wave "THE THIRD SHOT"`
