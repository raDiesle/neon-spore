## `d295769` — every editing panel put away

> with the editing panels put away, is the director's field big enough to judge a wave — and does anything shift under you when you open one again

- **badge** implementation
- **subject** the director's editing panels, collapsible one way for a click and another for a URL
- **changed** WAVES, BRIEFING, PAIR, PRESET, TUNING, BRUSH and MAP each got a collapse handle; `?closed=all` (or a list of ids) opens the page pre-collapsed with no mouse
- **decide** with everything put away, is the field roomy enough to actually judge a wave, and does it hold still when a panel reopens?
- **before** the four fixed panels beside the field, always fully open
- **after** any panel collapsed to its heading, reopened by a click; the field's own rect never changes
- **where** `bun run dev`, or `http://localhost:4174/?closed=all` for the pre-collapsed view
