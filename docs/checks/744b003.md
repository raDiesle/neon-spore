## `744b003` — one check per landing

> opening the check list, does it read as a short list of decisions you could make in one sitting, rather than as a log of everything that landed

- **badge** implementation
- **subject** the outstanding-check list itself, the one the director shows
- **changed** a landing now writes one trailer by default, not one per thing it touched
- **decide** does the list read as a handful of decisions, or as a log of every change
- **before** `bun run checks` / the director's `⚑ TO CHECK`, as it read before this landing
- **after** the same view, after future lanes have landed under the new rule
- **where** `bun run checks`, or `DIRECTOR_HOST=127.0.0.1 bun run dev`, `⚑ TO CHECK`
