## `3a00231` — every check short enough to answer standing up

> is every row of the check list now short enough to answer while standing up?

- **badge** implementation
- **subject** the restatement fields under every check in `bun run checks`
- **changed** subject/changed/decide were paragraphs on two of the six live entries; all now fit 15/25/25 words, and a `[concept]`/`[implementation]` badge prints first
- **decide** reading the list standing up, does each restated entry answer in two minutes, and is it obvious at a glance whether it is a proposal or a shipped change?
- **before** paragraph-length `changed`/`decide`, no badge — `restated — subject` first
- **after** capped fields, `[badge] restated — subject` first
- **where** `bun run checks`, or the director's TO CHECK sheet
