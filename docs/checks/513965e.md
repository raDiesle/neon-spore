## `513965e` — a wave editor field that grows, and a button instead of an empty guide

> open a wave with a long guide and one with none — does every field show all of its text without scrolling, and does the empty one offer a single button instead of blank boxes

- **badge** implementation
- **subject** the four text fields in the WAVE tab, and the empty-guide case
- **changed** every field now grows to fit what is typed into it, in place of scrolling inside a fixed box; a wave with no guide shows one ADD GUIDE button instead of three empty fields
- **decide** does typing a long line ever hide text behind a scrollbar, and does the button reveal blank fields cleanly in place of itself?
- **before** a two-row box that scrolled once text ran past it, and three empty boxes on a wave with nothing to say
- **after** a box that grows line by line as you type, and a single ADD GUIDE button that swaps itself for the fields
- **where** `DIRECTOR_HOST=127.0.0.1 bun run dev`, WAVE tab, SENTENCE and GUIDE fields — try a wave like FIRST STEP (has a guide) beside one like ALTERNATING (does not)
