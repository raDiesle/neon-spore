## `7ce974d` — TO CHECK shows implementations, never concepts

> open TO CHECK — is every row something the game or the tool now does differently, with no proposals mixed in?

- **badge** implementation
- **subject** the "to check" list, in the terminal and in the director
- **changed** rows for proposals nothing ships yet no longer appear or count on the list; ordinary rows are unaffected
- **decide** does every remaining row read as something the game or tool now does, with no proposal waiting on taste mixed in
- **before** the same list, with a proposal for a look not yet built sitting among finished changes
- **after** the same list, only rows for things that actually shipped
- **where** `DIRECTOR_HOST=127.0.0.1 bun run dev`, header ⚑ TO CHECK button
