## `7f541db` — a label on every parked idea

> scrolling the PARKED tab, can you tell what kind of thing each idea is without reading its paragraphs?

- **subject** the PARKED tab in the director, and the `docs/parked.md` file it reads — the list of things a session noticed and set aside
- **changed** all forty-five entries were plain paragraphs in the order a session happened to notice them, so the tab was a wall of text nobody could sort by eye. Every entry now carries one short line — what kind of thing it is (a mechanic, a creature or boss, a graphics idea, a sound, a tool fix, a performance finding, a correctness worry, or a documentation gap) and how far along it is (an idea, a worked-out design, or an already-built thing that wants improving). The one entry about an unbuilt boss moved to its own group at the bottom of the file, flagged as the least urgent thing to pick up next
- **decide** open the PARKED tab and scroll it without reading any paragraph — can you tell, entry by entry, what kind of idea each one is and whether it is just a thought, a worked-out plan, or a live fix to something already shipped? And does the boss idea read as clearly set aside at the bottom rather than mixed in with everything else?
- **before** forty-five entries, each three paragraphs of prose and nothing else, in the order a session happened to write them
- **after** every entry carries a short label under its date, and the one boss idea sits by itself at the bottom under its own heading
- **where** `bun run dev` (or `bun run preview`) → director → PARKED tab
