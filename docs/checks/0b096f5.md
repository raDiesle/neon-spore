## `0b096f5` — the alternatives page's impacts repeat

> on the alternatives page, does the rock hit the shield over and over with a pause between — long enough that you can see what the two sides do differently?

- **badge** implementation
- **subject** the cadence of event-shaped poses on the ALTERNATIVES page — WARD · DEFLECTED and SHOT · BEING LAID
- **changed** an event pose's impact used to repeat on the wave's own clear-and-rest timing rather than on any deliberate rhythm — measured at once every ~10.6s for the ward (the rock's own fall dwarfs any rest beat) and once every ~1.2s for the shot, neither legible as "the same thing happening again". `pose-kit.ts`'s new `Pose.cadenceSeconds` makes an event pose replay its own built moment on a fixed clock instead, at the owner's number: two seconds
- **decide** watching the shield:ward row or the cannon:shot row for twenty seconds or so, does the impact repeat with a pause long enough to compare what the two sides did differently before the next one lands, without waiting so long that you have forgotten the last one?
- **before** the ward hit once and went quiet for the rest of the run; the shot strobed almost continuously
- **after** either impact repeats roughly every two seconds
- **where** `DIRECTOR_HOST=127.0.0.1 bun run dev` → ALTERNATIVES, the shield:ward row or the cannon:shot row
