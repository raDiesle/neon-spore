## `0980374` — cards get their wave, and the list says so

> looking at the wave list, can you tell at a glance which waves teach something, which carry a boss, and which change the controls?

- **badge** implementation
- **subject** wave list gets a third mark for waves that open on a card
- **changed** boss and control-set marks existed; a wave that opens on a briefing card now carries its own ✎ mark, derived from `waves.ts`
- **decide** scanning the wave list, do the three marks read as three different facts, or blur into one dot?
- **before** two marks in the list: gold boss glyph, cyan ⎈ control glyph
- **after** a third, mint ✎ card mark on any wave `cardFirstWave` assigns something to
- **where** `bun run dev`, the wave list on the left
