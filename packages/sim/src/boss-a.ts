import type { World } from "./world.js";

/**
 * Variant A's whole choreography for the queen, written against the same
 * design as variant B, so that the two can be played side by side and
 * compared. Written in a later task.
 */
export function stepBossA(_world: World): void {
  // No choreography yet.
}
