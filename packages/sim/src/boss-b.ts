import type { World } from "./world.js";

/**
 * Variant B's whole choreography for the queen, written against the same
 * design as variant A, so that the two can be played side by side and
 * compared. Written in a later task.
 */
export function stepBossB(_world: World): void {
  // No choreography yet.
}
